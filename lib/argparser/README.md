# argparser

argparser is a utility which facilitates argument parsing, allowing for documenting the caller
program itself and its options and subcommands, while providing default behaviors for common Unix
command-line programs (such as assisting the user on its usage when given a help option).
